name: ci

on:
  push:
  pull_request:

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: prefix-dev/setup-pixi@v0.8.1
        with:
          environments: dev
          cache: true
      - name: Lint
        run: pixi run -e dev lint
      - name: Notebooks in sync (student .py + .ipynb twins generated from solutions/)
        run: pixi run -e dev build-notebooks --check
      - name: Test (mock data only — no GPU, no torch, no real files)
        run: pixi run -e dev test
